# Gen2Train 전용 추가 패키지.
# torch/accelerate/transformers/diffusers/xformers/psutil 등은 kohya_ss의 공유 venv
# (run.bat이 자동으로 찾는다 - Gen2Train과 kohya_ss/kohya_ss가 같은 상위 폴더 아래
# 나란히 있어야 함, 없으면 다른 python 환경으로 폴백)에 이미 설치되어 있다고 가정하므로
# 여기서는 UI용 패키지만 추가한다.
PySide6>=6.11.0

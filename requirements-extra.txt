# Gen2Train 전용 추가 패키지.
# torch/accelerate/transformers/diffusers/xformers 등 학습 관련 패키지는 sd-scripts/
# requirements.txt에 있다. kohya_ss의 공유 venv(Gen2Train과 kohya_ss/kohya_ss가 같은
# 상위 폴더 아래 나란히 있으면 run.bat/setup.py가 자동으로 찾아 재사용한다)에 이미
# 설치되어 있을 수도 있지만, 없으면 setup.py/setup.bat이 이 파일과 sd-scripts/
# requirements.txt, torch를 전부 설치해 이 프로젝트 전용 venv를 새로 만든다.
PySide6>=6.11.0
psutil>=5.9.0
